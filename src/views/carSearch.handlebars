<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Car Search Results</title>
    <link rel="stylesheet" href="/public/index.css">
</head>

<body>
    <div class="container mt-4">

        {{> searchBar}}

        <h1>Car Search Results</h1>

        {{#if error}}
            <p style="color: red;">{{error}}</p>
        {{/if}}

        {{#if results.length}}
            <ul class="list-group">
                {{#each results}}
                    <a href="/api/listings/search/{{listingId}}" class="listing-link">
                        {{this.make}} {{this.model}} ({{this.year}}) - ${{this.price}}
                    </a>
                {{/each}}
            </ul>
        {{else}}
            <p>No cars found matching your criteria.</p>
        {{/if}}
    </div>
    
    <a href="/">Return to Homepage</a>
</body>

</html>
<div class="search-bar-wrapper shadow d-flex justify-content-center">
  <div class="search-bar">
    {{> searchBar}}
  </div>
</div>
<div class="search-main">
  {{#if error}}
  <p style="color: red">{{ error }}</p>
  {{/if}}

  <ul class="list-group container card p-3 search-grid" id="search-grid"></ul>
  {{!-- {{#if results.length}}
  <ul class="list-group container card p-3 search-grid">
    {{#each results}}
    <a href="/listings/{{ _id }}" class="text-decoration-none text-dark">
      <li class="card shadow p-2">
        <div class="d-flex justify-content-between">
          <div>
            <h2 class="fs-5">{{ car.make }} {{ car.model }}</h2>
            <h3 class="fs-6 text-secondary">{{ car.year }} {{ car.color }}</h3>
          </div>
          <h4 class="text-success">{{currency price }}</h4>
        </div>

        <img src="{{ image }}" alt="" class="img-thumbnail" />
      </li>
    </a>
    {{/each}}
  </ul>
  {{else}}
  <p>No cars found matching your criteria.</p>
  {{/if}} --}}
</div>

<a href="/">Return to Homepage</a>

<script>
  const listings = {{{json results}}}
</script>
<script src="/scripts/search.js"></script>
